export class Timing {

    targets: Set<string>;

    constructor() {

    }

    create({ duration, transform }: { duration: number, transform: (x: number) => number}) {
        let timeSum = 0;

    }

}